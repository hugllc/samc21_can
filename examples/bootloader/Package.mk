

# This is the name of the package.  It will appear as the name of the archive files.
PACKAGE_NAME := bootloader



# The version is in a hex coded decimal.  It is actually a decimal number, but it is put into the
# endpoints as a hex number so that it will display correctly.

PACKAGE_VERSION       := 0x0
PACKAGE_VERSION_SMALL := 0x1
PACKAGE_VERSION_TINY  := 0x0



